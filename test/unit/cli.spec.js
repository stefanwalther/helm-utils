
describe('[unit] - cli.js', () => {
  describe('get-images', () => {
    it('throws an error if pointing to a non-existing resource.'); // You'll get a Z_DATA_ERROR exception which has to be handled.
  });
});
